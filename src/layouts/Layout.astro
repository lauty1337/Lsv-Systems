---
import '../styles/global.css';
import Header from '../sections/Header.astro';
import Footer from '../sections/Footer.astro';


---
<!doctype html>
<html lang="es" class="scroll-smooth ">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" href="/LSV.ico" />
		<title>LSV Systems - Desarrollo web y automatización de procesos</title>
		<script is:inline>
			(() => {
				const stored = localStorage.getItem('theme');
				const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
				const isDark = stored ? stored === 'dark' : prefersDark;
				document.documentElement.classList.toggle('dark', isDark);
			})();
		</script>
	</head>
	<body class="min-h-dvh flex flex-col text-black dark:text-white dark:bg-black transition-colors duration-300 ease-out">
		<Header />	
		<main class="flex-1 flex flex-col">
			<slot />
		</main>
		<Footer />

		<script>
			import '../scripts/lenis.js';
		</script>
	</body>
</html>


